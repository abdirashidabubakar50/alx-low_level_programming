#include <stdio.h>
/**
 * main - Entry into the main program
 * Function that prints the name of the program it was compiles from
 *
 * Return: return 0 Always success
*/
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
